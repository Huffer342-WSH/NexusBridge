# TASK.md

## 当前置顶开发计划

目标：把 NexusBridge 从“站点抓取 + 手动发送 + qB 状态同步”推进到“订阅自动下载、已有任务治理、自动清理、媒体硬链接整理”的闭环。

执行要求：

- 开始非平凡开发前，先确认当前置顶计划和推荐执行顺序。
- 以用户最新要求为准，每次选择一个小而完整、可测试的纵向切片推进，不跨越过多模块或混入无关重构。
- 完成一个单元时同步测试、文档，以及必要的 `TASK.md` 状态或计划调整；涉及 API、配置、数据库或 UI 时必须同步更新相关文档和测试。
- 当用户要求与当前计划不一致，或存在范围、优先级冲突时，先说明差异并与用户确认，确认后再执行或更新 `TASK.md`。
- 如果实现过程中发现计划已过时，优先把 `TASK.md` 更新到可继续执行的状态。
- 规则类功能先做 dry-run/preview，再做实际执行。
- 自动删除、移动、重命名、硬链接等会改变用户数据的功能，必须有明确的启用开关、日志和可解释的候选原因。

## 0. act 本地 GitHub Actions 打包

状态：已完成（2026-07-18 临时插入的独立工具链单元）。Windows + Docker Desktop 已实测运行 ARM64 容器和 `act` `linux-arm64` job，两个压缩包均成功生成，ELF `Machine` 均为 `AArch64`。

目标：在 Windows + Docker Desktop 上通过 `act` 复用 `build-release.yml`，实际打包 Linux ARM64 版本，同时保留 GitHub 原生 ARM64 runner 作为发布验证路径。

验收：

- PowerShell 入口支持 `linux-arm64` dry-run 和完整打包。
- Docker Desktop 上的 ARM64 容器报告 `aarch64`。
- 生成 WebUI/CLI 服务版和 Desktop 桌面版的 Linux aarch64 压缩包。
- README 和测试文档明确仿真性能与 Windows job 限制。

## 1. 订阅规则自动添加种子

状态：已完成（2026-07-15）。筛选规则与订阅已拆分为独立实体，站点调度、候选独占、qB 下载计划、配额、预览/批量执行、WebUI、API、迁移和测试闭环均已落地。

目标：基于站点缓存和筛选规则，自动把命中的种子发送到 qBittorrent，并自动设置保存路径、标签和文件名。

功能拆分：

- 订阅规则模型：
  - 匹配条件：站点、分类、子分类、`tags`、`tag_ids`、关键词、促销、体积、做种/下载/完成数、发布时间。
  - 下载配置：qB 分类、保存路径模板、标签模板、文件名模板、是否暂停、最大并发、每日数量限制。
  - 默认关闭自动添加，必须由用户显式开启。
- 路径自动设置：
  - 支持按站点、分类、子分类、规则名、标题解析结果生成路径。
  - 提供预览接口，展示最终 qB `savepath` 和原因。
- tag 自动设置：
  - 合并全局默认 tag、规则 tag、站点标签、解析出的 subtitle tags。
  - 去重、长度限制、非法字符处理。
- 文件名自动设置：
  - 下载前生成文件名预览。
  - 优先使用用户模板；没有模板时保留站点原始 torrent 名称。
  - 避免覆盖同名任务，冲突时给出稳定后缀或拒绝原因。
- 发送状态：
  - 保存 `pending/sent/exists/failed/skipped`、qB hash、失败原因、重试次数、最后尝试时间。
  - 同一 `site_id + torrent_id + rule_name` 幂等，不重复发送。

验收：

- fake qB 服务覆盖 add 表单字段、路径、tag、分类、暂停状态和失败处理。
- SQLite 持久化覆盖规则、发送状态和幂等。
- WebUI/API 能创建规则、预览命中结果、手动执行一次。

## 2. 从保留文件夹恢复 qB 种子

状态：文件管理器与文件大小索引纵向切片已完成（2026-07-18）。完整多页检索和搜索记录持久化后续补齐。

目标：qB 任务意外删除但数据目录仍存在时，按完整文件大小集合找回原种并重建任务，同时保持磁盘文件结构不变。

当前后端切片：

- 数据库、站点网页和直接 URL 最终都要求完整文件大小多重集合一致，包括文件数量和同尺寸出现次数；路径和文件名只用于优先建立映射，piece SHA1 预校验暂不实现。
- SQLite 使用 `(file_size, site_id, torrent_id)` 倒排索引缩小数据库候选，并保存每个大小的出现次数、torrent 文件总数和总大小。旧 BLOB 不后台补齐，由用户在文件页手动重建一次；之后保存、覆盖和删除 torrent BLOB 时事务内自动维护。
- 支持仅查询本地数据库、仅通过站点网页搜索、先数据库再站点网页三种模式；站点可选，未指定时遍历全部已配置站点。站点按文件夹完整名、去扩展名、括号标题和分词变体合并最多 5 个第一页检索结果，先用站点展示总大小的安全容差缩小 torrent 文件下载候选，再持久化搜索结果与匹配范围内的 torrent 文件。
- 预览只返回精确匹配候选、来源和检索诊断，不改变 qB 状态。
- 所有来源都以目标路径的父目录作为 `savepath` 且 `root_folder=false`，关闭 `autoTMM`、设置 `paused=true` 和 `skip_checking=true`；添加后只通过 qB `renameFile` 映射到磁盘已有路径，不移动、复制或重命名磁盘文件，完成分类及路径设置后再显式强制校验。
- 恢复执行不要求调用方预先知道 `torrent_id`；后端按目录、可选站点和模式自动查找，并在执行前再次验证完整大小集合和文件映射。候选不唯一时拒绝自动选择，`torrent_id` 仅保留为可选消歧字段。
- 文件和目录都可直接作为恢复目标；文件管理器从 `save_path` 下的唯一内容根开始标记 qB 归属，覆盖其后代目录和文件但不标记同级无关项；同时提供独立返回/前进历史并接管鼠标侧键，右键弹窗提供站点、检索模式、直接 torrent URL 和分类选择。
- 直接 URL 自动匹配站点主域名或 `request_rules`，私站下载复用统一 Cookie 请求入口。强制校验未进入 checking 状态时等待并重试，只有校验后保持暂停且 100% 完成才启动做种；否则保留唯一任务并允许用户显式启动或仅删除任务。保存目录与 qB 分类目录完全一致时自动设置分类。
- 高级扫描默认数据库模式、递归深度 1、最多 200 项，硬上限为深度 5 和 500 项；扫描只读，用户可显式启用数据库未命中后的站点网页搜索，并在预览后一次确认串行自动恢复全部唯一候选。相同路径或 torrent 自动去重，单项失败不阻断后续项，校验失败任务保持暂停等待用户处理。

后续完整功能：

- 站点多页检索、中断/续查与独立搜索记录持久化。

验收：

- 仅保留真实 qB 恢复闭环测试；目标 hash 通过 `NEXUSBRIDGE_TEST_RECOVERY_HASH` 显式传入，子测试覆盖站点、数据库、数据库优先和直接 URL，并验证文件管理器归属、分类推断和只读扫描。
- 真实试验删除任务时必须使用 `deleteFiles=false`，恢复后确认环境变量指定的同一 hash 重新出现；自动批量入口分别覆盖网页搜索关闭和开启。

实验结果：通过标题变体“淫乳聖女アネモネ”检索到 KamePT `torrent_id=19638`，下载 torrent 计算得到目标 hash。2026-07-18 使用 `907474298dc8c4c3443486b1f38274be33f72a40` 真实运行站点、数据库、数据库优先、直接 URL，以及网页搜索关闭/开启的自动批量恢复六个子测试，均以 `deleteFiles=false` 删除任务后恢复同一 hash，并通过强制校验和路径断言。

## 3. qB 已有任务匹配与重新设置

状态：下一推荐单元。

目标：把 qB 中已经存在且能和本地数据库匹配的任务纳入治理，自动重新设置路径、tag、分类等。

功能拆分：

- 匹配策略：
  - 优先使用本地 `.torrent` 解析出的 v1/v2 info hash 匹配。
  - hash 不可用时，只允许显式开启弱匹配，并记录原因。
- 重设策略：
  - dry-run 输出当前 qB 状态、目标路径、目标 tag、目标分类、差异原因。
  - apply 时调用 qB API 设置分类、tag；路径迁移需要单独开关。
- 任务来源：
  - 支持单个种子手动重设。
  - 支持按规则批量扫描并重设。

验收：

- qB fake/real 测试覆盖 hash 匹配、弱匹配禁用、tag/category 更新。
- WebUI 展示可匹配、已匹配、冲突、不可匹配状态。

## 4. 自动删除规则

目标：根据磁盘剩余空间、qB 总占用空间和分类继承规则，自动生成删除候选并在用户授权后执行。

功能拆分：

- 空间指标：
  - 读取目标磁盘剩余空间。
  - 读取 qB 总占用空间、单任务大小、完成状态、做种时间、分享率、最后活动时间。
- 规则层级：
  - 全局默认规则。
  - 分类规则。
  - 子分类规则，默认继承分类。
  - 单个种子规则，默认继承子分类或分类。
- 删除条件：
  - 最小剩余空间阈值。
  - qB 总占用空间阈值。
  - 完成后保留时间。
  - 最小分享率/最小做种时间。
  - 分类、tag、规则来源、是否已硬链接整理。
- 执行策略：
  - 第一阶段只做候选 preview 和手动确认。
  - 自动执行必须有独立开关。
  - 删除任务时明确区分“仅从 qB 移除”和“同时删除文件”。

验收：

- 规则继承单测：全局 -> 分类 -> 子分类 -> 种子。
- 候选排序和拒绝原因稳定。
- 删除执行必须可审计，并覆盖安全开关测试。

## 5. 媒体文件夹解析与硬链接整理

目标：自动解析下载内容的多层文件夹结构，把媒体文件硬链接到媒体服务器目录，结构为 `作品文件夹/作品`。

功能拆分：

- 文件树解析：
  - 支持单文件、单层目录、多层目录、合集目录。
  - 识别主媒体文件、字幕、封面、nfo、样本文件。
  - 过滤临时文件、无关小文件和 qB 未完成文件。
- 作品目录生成：
  - 基于规则、标题解析、详情页字段和用户模板生成 `作品文件夹/作品`。
  - 合集篇允许生成多个作品子项或保留合集目录。
- 硬链接计划：
  - dry-run 展示源文件、目标路径、冲突处理、是否可硬链接。
  - 跨盘无法硬链接时给出明确错误，不静默复制。
  - 目标已存在时支持跳过、覆盖拒绝、追加后缀。
- 整理状态：
  - 保存待整理、已硬链接、失败、跳过、最后错误。
  - 和自动删除规则联动：已整理任务可进入更激进的删除候选。

验收：

- fixture 文件树覆盖单文件、多层目录、合集、字幕/封面、冲突目标。
- 使用临时目录测试硬链接创建和失败路径。
- WebUI/API 提供整理预览和手动执行。

## 6. 调度与安全

目标：把自动添加、已有任务重设、自动删除和硬链接整理纳入可控调度。

功能拆分：

- 任务类型：
  - 站点刷新。
  - 订阅规则计算与自动添加。
  - qB 已有任务匹配与重设。
  - 删除候选生成与执行。
  - 下载完成后的硬链接整理。
- 调度约束：
  - 单站点和单规则防重入。
  - context cancellation。
  - 失败重试和退避。
  - 最近执行记录、耗时、错误、影响数量。
- 安全策略：
  - 所有自动执行默认关闭。
  - destructive 操作单独开关。
  - 日志和 API 不泄露 cookie、token、passkey、密码。

验收：

- fake clock 或短 interval 测试调度。
- 并发保护和取消测试。
- 每类任务都有可查询的执行记录。

## 7. WebUI 与 API

目标：让上述自动化能力可以被用户配置、预览、执行和审计。

优先页面：

- 订阅规则编辑与命中预览。
- 自动添加配置：路径、tag、文件名模板。
- qB 已有任务匹配与重设预览。
- 删除规则层级编辑：全局、分类、子分类、种子。
- 删除候选列表和执行记录。
- 硬链接整理预览、执行记录和失败修复入口。

API 要求：

- 新增接口先写入 `docs/api/requests.md`，再实现路由、类型、OpenAPI、测试和前端调用。
- 预览接口不改变状态；执行接口必须写审计记录。

## 推荐执行顺序

当前状态：任务 1 已完成；任务 2 已完成后端恢复、WebUI 文件管理器、直接 URL 和有界扫描，其多页检索阶段留待后续。

1. 订阅规则模型和自动添加 dry-run。
2. qB 添加时自动设置路径、tag、文件名。
3. 自动添加执行与发送状态持久化。
4. 从保留文件夹执行数据库优先匹配和 qB 恢复（后端）。
5. 恢复功能的 WebUI 文件管理器和有界丢失任务扫描。
6. 恢复功能的多页站点检索和搜索持久化。
7. qB 已有任务 hash 匹配和 tag/category 重设。
8. 删除规则模型、继承和候选 preview。
9. 删除执行的安全开关和审计。
10. 文件树解析与硬链接 dry-run。
11. 硬链接执行和整理状态。
12. 调度器整合。
13. WebUI 完整配置和审计页面。
